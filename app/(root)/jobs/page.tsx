import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Jobs",
  description: "This is all jobs requirements",
};

const Jobs = () => {
  return <div>Jobs</div>;
};

export default Jobs;
